<h1>Login</h1>

<%= form_for(@user) do |f| %>
  <%= f.label :user_id %> <%= f.text_field :user_id, autofocus: true %><br />
  <%= f.label :password %> <%= f.password_field :password %><br />

  <p><%= f.submit %></p>
<% end %>
